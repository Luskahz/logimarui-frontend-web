"use client";

import { useEffect, useMemo, useState } from "react";
import { ListFilter, RefreshCw, Search, Settings2, X } from "lucide-react";
import type { EChartsOption } from "echarts";
import DtoEChart from "@/features/dpo/ui/dto/DtoEChart";
import { DtoBadge, DtoButton, DtoMetricCard, DtoPanel, DtoStatePanel } from "@/features/dpo/ui/dto/DtoPrimitives";
import { computeDtoTracking, computeDtoTrackingMonthSummary, computeDtoTrackingYear, getDtoTrackingYears } from "@/features/dpo/lib/dtoTracking";
import { formatDtoDate, formatDtoNumber, formatDtoPercentage, normalizeSearchText, parseDtoDate } from "@/features/dpo/lib/dtoFormatters";
import type { DtoFormDetail, DtoTrackingContext, DtoTrackingMonthCell, DtoTrackingMonthStatus, DtoTrackingYearRow, WorkforceFilterCatalog, WorkforceTrackingFilter } from "@/features/dpo/model/dtoTypes";
import { useFormManagerConfig } from "@/features/dpo/model/formManagerConfig";
import { Typography } from "@/shared/ui/typography";

const MONTHS = ["Janeiro", "Fevereiro", "Março", "Abril", "Maio", "Junho", "Julho", "Agosto", "Setembro", "Outubro", "Novembro", "Dezembro"];
const STATUS_LABELS: Record<DtoTrackingMonthStatus, string> = { realized: "Realizado", covered: "Coberto", due: "Vencendo", missed: "Pendente", realizedLate: "Realizado fora do prazo", notApplicable: "Não aplicável", future: "Futuro" };
const STATUS_COLORS: Record<DtoTrackingMonthStatus, string> = { realized: "#0f9b8e", covered: "#70c9c0", due: "#d99a18", missed: "#dc4f4f", realizedLate: "#e67932", notApplicable: "#64748b", future: "#94a3b8" };
const STATUS_ORDER: DtoTrackingMonthStatus[] = ["realized", "covered", "due", "missed", "realizedLate", "notApplicable", "future"];
type TrackingFilters = { location: string; employeeKey: string; area: string; status: "" | DtoTrackingMonthStatus; workforceFilterId: string };
const EMPTY_FILTERS: TrackingFilters = { location: "", employeeKey: "", area: "", status: "", workforceFilterId: "" };

function escapeHtml(value: string): string { return value.replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character] || character); }
function cellDescription(cell: DtoTrackingMonthCell): string {
  if (cell.status === "covered" && cell.coverageRealization && cell.coverageEndDate) return `Coberto pela realização de ${formatDtoDate(cell.coverageRealization)} até ${formatDtoDate(cell.coverageEndDate)}.`;
  if (cell.status === "due" && cell.dueDate) return `Prazo vence em ${formatDtoDate(cell.dueDate)}.`;
  if (cell.status === "missed" && cell.dueDate) return `Prazo venceu em ${formatDtoDate(cell.dueDate)} sem realização dentro do período.`;
  if (cell.status === "realizedLate" && cell.realizations[0] && cell.dueDate) return `Realização registrada em ${formatDtoDate(cell.realizations[0])} após vencimento em ${formatDtoDate(cell.dueDate)}.`;
  if (cell.status === "realized") return `Realização registrada em ${cell.realizations.map((date) => formatDtoDate(date)).join(", ")}.`;
  return STATUS_LABELS[cell.status];
}

function FilterDialog({ catalog, filters, onChange, onClose, rows, sharedFilters }: { catalog: WorkforceFilterCatalog | null; filters: TrackingFilters; onChange: (filters: TrackingFilters) => void; onClose: () => void; rows: DtoTrackingYearRow[]; sharedFilters: WorkforceTrackingFilter[] }) {
  useEffect(() => { const listener = (event: KeyboardEvent) => event.key === "Escape" && onClose(); window.addEventListener("keydown", listener); return () => window.removeEventListener("keydown", listener); }, [onClose]);
  return <div role="presentation" onMouseDown={(event) => event.currentTarget === event.target && onClose()} className="fixed inset-0 z-[90] flex items-end justify-center bg-[var(--shell-overlay)] p-0 backdrop-blur-sm sm:items-center sm:p-5"><section role="dialog" aria-modal="true" aria-labelledby="tracking-filters-title" className="w-full rounded-t-[30px] border border-[color:var(--shell-line)] bg-[var(--shell-surface-strong)] p-5 shadow-2xl sm:max-w-2xl sm:rounded-[30px] sm:p-6"><header className="flex items-start justify-between gap-4"><div><Typography variant="overline">Acompanhamento</Typography><Typography id="tracking-filters-title" as="h2" variant="sectionTitle" className="mt-2">Filtros</Typography></div><DtoButton size="icon-sm" aria-label="Fechar filtros" onClick={onClose}><X aria-hidden="true" /></DtoButton></header><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-[var(--shell-text)]">Local<select value={filters.location} onChange={(event) => onChange({ ...filters, location: event.target.value })} className="mt-2 h-11 w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm"><option value="">Todos os locais</option>{(catalog?.locations || []).map((item) => <option key={item.name} value={item.name}>{item.name}</option>)}</select></label><label className="text-sm font-semibold text-[var(--shell-text)]">Colaborador<select value={filters.employeeKey} onChange={(event) => onChange({ ...filters, employeeKey: event.target.value })} className="mt-2 h-11 w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm"><option value="">Todos os colaboradores</option>{rows.map((row) => <option key={row.subject.key} value={row.subject.key}>{row.subject.name}</option>)}</select></label><label className="text-sm font-semibold text-[var(--shell-text)]">Setor<select value={filters.area} onChange={(event) => onChange({ ...filters, area: event.target.value })} className="mt-2 h-11 w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm"><option value="">Todos os setores</option>{(catalog?.areas || []).map((item) => <option key={item.name} value={item.name}>{item.name}</option>)}</select></label><label className="text-sm font-semibold text-[var(--shell-text)]">Status de realização<select value={filters.status} onChange={(event) => onChange({ ...filters, status: event.target.value as TrackingFilters["status"] })} className="mt-2 h-11 w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm"><option value="">Todos</option>{STATUS_ORDER.filter((status) => !["future", "notApplicable"].includes(status)).map((status) => <option key={status} value={status}>{STATUS_LABELS[status]}</option>)}</select></label><label className="text-sm font-semibold text-[var(--shell-text)] sm:col-span-2">Filtro público<select value={filters.workforceFilterId} onChange={(event) => onChange({ ...filters, workforceFilterId: event.target.value })} className="mt-2 h-11 w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm"><option value="">Nenhum filtro público</option>{sharedFilters.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label></div><footer className="mt-6 flex justify-end gap-2"><DtoButton onClick={() => onChange(EMPTY_FILTERS)}>Limpar filtros</DtoButton><DtoButton tone="accent" onClick={onClose}>Aplicar filtros</DtoButton></footer></section></div>;
}

export default function DtoTrackingPanel({ detail, onConfigure }: { detail: DtoFormDetail; onConfigure: () => void }) {
  const { api } = useFormManagerConfig(); const years = useMemo(() => getDtoTrackingYears(detail), [detail]); const initialYear = years.at(-1) || new Date().getFullYear(); const sourceEnd = useMemo(() => parseDtoDate(detail.source_period_end) || new Date(), [detail.source_period_end]);
  const [year, setYear] = useState(initialYear); const [month, setMonth] = useState(sourceEnd.getFullYear() === initialYear ? sourceEnd.getMonth() : 11); const [search, setSearch] = useState(""); const [filters, setFilters] = useState<TrackingFilters>(EMPTY_FILTERS); const [filtersOpen, setFiltersOpen] = useState(false); const [hoverInfo, setHoverInfo] = useState(""); const [contextResult, setContextResult] = useState<{ key: string; context: DtoTrackingContext | null; error: string | null } | null>(null); const [catalog, setCatalog] = useState<WorkforceFilterCatalog | null>(null); const [sharedFilters, setSharedFilters] = useState<WorkforceTrackingFilter[]>([]);
  const needsContext = detail.configuration.tracking.mode === "COLLABORATOR"; const contextKey = needsContext ? `${detail.form.id}:${detail.configuration.revision}:${filters.workforceFilterId}` : null;
  useEffect(() => { if (!contextKey) return; const controller = new AbortController(); void api.getTrackingContext(detail.form.id, { workforceFilterId: filters.workforceFilterId || undefined }, controller.signal).then((context) => setContextResult({ key: contextKey, context, error: null })).catch((error: unknown) => { if (error instanceof Error && error.name === "AbortError") return; setContextResult({ key: contextKey, context: null, error: error instanceof Error ? error.message : "Não foi possível consultar o cadastro." }); }); return () => controller.abort(); }, [api, contextKey, detail.form.id, filters.workforceFilterId]);
  useEffect(() => { if (!needsContext) return; const controller = new AbortController(); void Promise.all([api.getWorkforceFilterCatalog(controller.signal), api.listWorkforceFilters(controller.signal)]).then(([nextCatalog, nextFilters]) => { setCatalog(nextCatalog); setSharedFilters(nextFilters); }).catch(() => { setCatalog(null); setSharedFilters([]); }); return () => controller.abort(); }, [api, needsContext]);
  const context = contextResult?.key === contextKey ? contextResult.context : null; const tracking = useMemo(() => computeDtoTracking(detail, context), [detail, context]); const annual = useMemo(() => computeDtoTrackingYear(detail, context, year), [detail, context, year]);
  const filteredRows = useMemo(() => annual.rows.filter((row) => { const query = normalizeSearchText(search); const cell = row.months[month]; return (!query || normalizeSearchText(`${row.subject.name} ${row.subject.function || ""}`).includes(query)) && (!filters.location || row.location === filters.location) && (!filters.employeeKey || row.subject.key === filters.employeeKey) && (!filters.area || row.area === filters.area) && (!filters.status || cell.status === filters.status); }), [annual.rows, filters, month, search]);
  const summary = useMemo(() => computeDtoTrackingMonthSummary(filteredRows, year, month), [filteredRows, year, month]); const activeFilters = [["location", filters.location], ["employeeKey", filters.employeeKey], ["area", filters.area], ["status", filters.status], ["workforceFilterId", filters.workforceFilterId]].filter(([, value]) => Boolean(value));
  if (!tracking.configured) return <DtoStatePanel title="Defina a forma de acompanhamento" description="Configure a periodicidade e a população para exibir a visão histórica." action={<DtoButton tone="accent" onClick={onConfigure}><Settings2 aria-hidden="true" /> Configurar acompanhamento</DtoButton>} />;
  if (needsContext && contextKey !== contextResult?.key) return <DtoStatePanel title="Consultando o cadastro de colaboradores" description="A população oficial e os vínculos das realizações estão sendo carregados." />;
  if (needsContext && contextResult?.error) return <DtoStatePanel title="Não foi possível montar o acompanhamento" description={contextResult.error} action={<DtoButton onClick={() => setFilters({ ...filters })}><RefreshCw aria-hidden="true" /> Tentar novamente</DtoButton>} />;
  const pieOption: EChartsOption = { color: [STATUS_COLORS.realized, STATUS_COLORS.covered, STATUS_COLORS.due, STATUS_COLORS.missed, STATUS_COLORS.realizedLate], tooltip: { trigger: "item" }, legend: { bottom: 0, textStyle: { color: "#94a3b8" } }, series: [{ type: "pie", radius: ["48%", "72%"], center: ["50%", "43%"], data: [{ name: "Realizado", value: summary.realized }, { name: "Coberto", value: summary.covered }, { name: "Vencendo", value: summary.due }, { name: "Pendente", value: summary.missed }, { name: "Realizado fora do prazo", value: summary.realizedLate }] }] };
  const matrixOption: EChartsOption = { animation: false, tooltip: { position: "top", formatter: (params: any) => { const data = params.data as [number, number, number]; const row = filteredRows[data[1]]; const cell = row.months[data[0]]; return `<strong>${escapeHtml(row.subject.name)}</strong><br/>${MONTHS[cell.month]}/${cell.year}<br/><strong>${STATUS_LABELS[cell.status]}</strong><br/>${escapeHtml(cellDescription(cell))}<br/>Função: ${escapeHtml(row.subject.function || "—")}<br/>Local: ${escapeHtml(row.location || "—")}<br/>Setor: ${escapeHtml(row.area || "—")}`; } }, grid: { top: 20, right: 22, bottom: 70, left: 180 }, xAxis: { type: "category", data: MONTHS, triggerEvent: true, axisLabel: { color: "#94a3b8" }, splitArea: { show: true } }, yAxis: { type: "category", data: filteredRows.map((row) => row.subject.name), triggerEvent: true, axisLabel: { color: "#94a3b8", width: 155, overflow: "truncate" }, splitArea: { show: true } }, visualMap: { show: false, pieces: STATUS_ORDER.map((status, index) => ({ value: index, color: STATUS_COLORS[status] })) }, dataZoom: [{ type: "inside", yAxisIndex: 0 }, { type: "slider", yAxisIndex: 0, right: 2, width: 12 }], series: [{ type: "heatmap", data: filteredRows.flatMap((row, rowIndex) => row.months.map((cell, cellMonth) => [cellMonth, rowIndex, STATUS_ORDER.indexOf(cell.status)])), label: { show: false }, emphasis: { itemStyle: { borderColor: "#f8fafc", borderWidth: 1 } } }] };
  const onMatrixEvent = (params: any) => { if (params?.seriesType === "heatmap") setMonth(Number(params.data?.[0])); if (params?.componentType === "xAxis") { const next = MONTHS.indexOf(params.value); if (next >= 0) setMonth(next); } };
  const onMatrixHover = (params: any) => { if (params?.componentType === "xAxis") { const index = MONTHS.indexOf(params.value); if (index >= 0) { const value = computeDtoTrackingMonthSummary(filteredRows, year, index); setHoverInfo(`${MONTHS[index]}/${year}: ${value.applicable} aplicáveis, ${value.realized + value.realizedLate} realizados, ${value.covered} cobertos e ${value.missed} pendentes.`); } } else if (params?.componentType === "yAxis") { const row = filteredRows.find((item) => item.subject.name === params.value); if (row) setHoverInfo(`${row.subject.name} · ${row.subject.function || "Função não informada"} · ${row.location || "Local não informado"} · Setor: ${row.area || "não informado"} · ${row.realizations} realização(ões) no ano.`); } };
  return <div className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><DtoBadge tone="accent">{tracking.mode === "ENVIRONMENT" ? "Acompanhamento por ambiente" : "Acompanhamento por colaborador"}</DtoBadge><select aria-label="Ano do acompanhamento" value={year} onChange={(event) => { const nextYear = Number(event.target.value); setYear(nextYear); setMonth(nextYear === sourceEnd.getFullYear() ? sourceEnd.getMonth() : 11); }} className="h-10 rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface)] px-3 text-sm font-semibold text-[var(--shell-text)]">{years.map((item) => <option key={item} value={item}>{item}</option>)}</select></div><section><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><DtoMetricCard label="Aplicáveis" value={formatDtoNumber(summary.applicable)} /><DtoMetricCard label="Realizados" tone="accent" value={formatDtoNumber(summary.realized + summary.realizedLate)} /><DtoMetricCard label="Cobertos" tone="accent" value={formatDtoNumber(summary.covered)} /><DtoMetricCard label="Pendentes" tone={summary.missed ? "danger" : "default"} value={formatDtoNumber(summary.missed + summary.due)} /><DtoMetricCard label="Cobertura" tone="accent" value={formatDtoPercentage(summary.coveragePercentage)} /></div></section><div className="grid gap-4 xl:grid-cols-[0.75fr_1.25fr]"><DtoPanel className="p-5 sm:p-6"><Typography variant="overline">Cobertura mensal</Typography><Typography as="h2" variant="cardTitle" className="mt-2">Situação em {MONTHS[month]}/{year}</Typography><DtoEChart ariaLabel={`Situação da periodicidade em ${MONTHS[month]} de ${year}`} className="mt-3 h-80" option={pieOption} /></DtoPanel><DtoPanel className="p-5 sm:p-6"><Typography variant="overline">Visão histórica</Typography><Typography as="h2" variant="cardTitle" className="mt-2">Matriz anual de acompanhamento</Typography><Typography variant="caption" className="mt-1">Clique em uma célula ou mês para alterar o período em foco.</Typography><DtoEChart ariaLabel={`Matriz anual para ${year}`} className="mt-3 h-[40rem] min-h-96" option={matrixOption} onEvents={{ click: onMatrixEvent, mouseover: onMatrixHover }} />{hoverInfo ? <p role="status" className="mt-3 text-xs leading-5 text-[var(--shell-muted)]">{hoverInfo}</p> : null}</DtoPanel></div><DtoPanel className="p-5 sm:p-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><Typography variant="overline">População do período</Typography><Typography as="h2" variant="cardTitle" className="mt-2">{MONTHS[month]}/{year}</Typography></div><div className="flex flex-1 flex-wrap justify-end gap-2"><label className="relative min-w-60 flex-1 sm:max-w-sm"><span className="sr-only">Buscar colaborador</span><Search aria-hidden="true" className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--shell-muted)]" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar colaborador" className="w-full rounded-2xl border border-[color:var(--shell-line)] bg-[var(--shell-surface-muted)] py-2.5 pl-10 pr-3 text-sm text-[var(--shell-text)]" /></label>{tracking.mode === "COLLABORATOR" ? <DtoButton onClick={() => setFiltersOpen(true)}><ListFilter aria-hidden="true" />Filtros{activeFilters.length ? ` (${activeFilters.length})` : ""}</DtoButton> : null}</div></div>{activeFilters.length ? <div className="mt-4 flex flex-wrap items-center gap-2">{activeFilters.map(([key, value]) => <button key={key} type="button" onClick={() => setFilters({ ...filters, [key]: "" })} className="rounded-full border border-[color:var(--shell-line)] bg-[var(--shell-surface-muted)] px-3 py-1 text-xs font-semibold text-[var(--shell-text)]">{key === "area" ? "Setor" : key === "employeeKey" ? "Colaborador" : key === "workforceFilterId" ? "Filtro público" : key === "status" ? "Status" : "Local"}: {key === "status" ? STATUS_LABELS[value as DtoTrackingMonthStatus] : String(value)} ×</button>)}<button type="button" className="text-xs font-semibold text-[var(--shell-accent)]" onClick={() => setFilters(EMPTY_FILTERS)}>Limpar filtros</button></div> : null}<p className="mt-5 text-sm text-[var(--shell-muted)]">{formatDtoNumber(filteredRows.length)} item(ns) no recorte. Passe o cursor sobre a matriz para ver realizações, cobertura, prazo e atraso.</p></DtoPanel>{filtersOpen ? <FilterDialog catalog={catalog} filters={filters} onChange={setFilters} onClose={() => setFiltersOpen(false)} rows={annual.rows} sharedFilters={sharedFilters} /> : null}</div>;
}
